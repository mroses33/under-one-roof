<%= simple_form_for [@experience, @item] do |f| %>
            <%= f.input :amount %>
            <%= f.input :name %>
            <div class="form-actions">
              <%= f.button :submit, class: "btn btn-primary" %>
            </div>
          <% end %>
